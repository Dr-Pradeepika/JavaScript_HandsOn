
// Print result of num1+num2 if operation is “add”
// Print result of num1-num2 if operation is “subtract”
// Print result of num1*num2 if operation is “multiply”
// Print result of num1/num2 if operation is “divide”
// Print result of num1%num2 if operation is “modulus”
// Else print “Invalid operation”

// Requirement: 
// Get UserInput: Number1, Number2
// define Function
// create var to get usernumbers
// creat var to add Number1 + Number2
// creat var to Substract Number1 - Number2
// creat var to modulus Number1 % Number2
// creat var to divide Number1 / Number2
// creat var to multiply Number1 * Number2
// prompt()

function getResult(number1, number2){
    var userInput1 = +document.getElementById("Number1").value;
    var userInput2 = +document.getElementById("Number2").value;
    if (var i= 0; i==number1 + number2) {
        prompt(i);
    } else {
        
    }

    prompt(userInput1 + userInput2);
    prompt(userInput1 / userInput2);
    prompt(userInput1 % userInput2);
    prompt(userInput1 * userInput2);
    prompt(userInput1 - userInput2);



}